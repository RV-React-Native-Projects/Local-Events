import { View } from 'react-native';
import AppText from '@components/AppText/AppText';
import { I{{pascalCase name }}Props } from './{{pascalCase name}}Types';
import { use{{pascalCase name }}Style } from './style';

export default function {{pascalCase name }}(props: I{{pascalCase name }}Props) {
  const {} = props;
  const { container } = use{{pascalCase name }}Style();
  return (
    <View style={container}>
      <AppText>{{pascalCase name }}</AppText>
    </View>
  );
}

