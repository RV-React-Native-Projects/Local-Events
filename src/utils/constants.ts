import { moderateScale } from '@themes/responsive';
import App from '../../app.json';

export const APP_NAME = App.displayName;
export const EMPTY_STRING = '';
export const INPUT_HEIGHT = moderateScale(50);
export const BUTTON_HEIGHT = moderateScale(50);
export const TAB_HEIGHT = moderateScale(80);
export const TAB_WIDTH = moderateScale(500);
export const NO_VALUE = '-';
export const DATE_FORMATE = 'YYYY-MM-DD';
export const ZERO = 0;
